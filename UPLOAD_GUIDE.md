# วิธีอัปไฟล์ YGPH METROPOLIS

ชุดนี้ใช้ชื่อไฟล์เดิมและ Path เดิม เพื่อให้แอปออฟไลน์กับ Service Worker ตามของใหม่ได้ถูกต้อง

## ก่อนอัป

1. เก็บสำเนาไฟล์เดิมไว้ก่อน
2. ถ้าได้รับเป็น ZIP ให้แตกไฟล์ `YGPH_METROPOLIS_UPLOAD.zip`
3. ตรวจว่าไฟล์หลักยังชื่อเดิม เช่น `index.html`, `app.js`, `highway-gate.js`, `flow-era.js`, `sw.js`

## ถ้าทำผ่าน GitHub

รันที่โฟลเดอร์หลัก:

```bash
npm run deploy:gate
```

ต้องผ่าน 3 ส่วน:

- เทสต์ทั้งหมดผ่าน
- JavaScript Syntax ผ่าน
- ภาษาไทย UTF-8 ผ่าน

Workflow `Phase 1 Deploy Gate` ทำงานแบบนี้:

- ตอนเปิดหรืออัปเดต Pull Request: ตรวจ gate อย่างเดียว ยังไม่ Deploy
- ตอน merge/push เข้า `main`: ตรวจ gate อีกครั้ง และถ้าผ่านจึง Deploy YGPH METROPOLIS ไป Cloudflare
- ถ้า gate ไม่ผ่าน งาน Deploy จะไม่เดินต่อ

ดังนั้นอย่าดูแค่ว่าไฟล์เข้า `main` แล้ว ให้ตรวจผล workflow/deploy ด้วยเมื่อทำได้

## ถ้าอัปด้วยวิธีอื่น

1. คัดลอกไฟล์ไปวางที่ Root ของโปรเจกต์
2. แทนที่ไฟล์ชื่อเดิม
3. ห้ามเติมเลขรุ่นต่อท้ายชื่อไฟล์
4. รัน `npm run deploy:gate` ก่อน Deploy ทุกครั้ง

## หลัง Deploy

1. เปิดแอปออนไลน์หนึ่งครั้ง เพื่อให้ Service Worker เห็นไฟล์รุ่นใหม่
2. เข้า “ตั้งค่าและเครื่องมือ” แล้วดูสถานะอัปเดต
3. ใช้รุ่นใหม่เมื่อพร้อม แล้วตรวจว่าข้อมูลเดิมยังอยู่
4. ล็อก–ปลดล็อก และปิด–เปิดแอปใหม่อีกครั้งเพื่อเช็กข้อมูลกับการทำงานออฟไลน์

ฐานข้อมูลยังใช้ `stock-pocket-secure`, DB version 1, store `kv`, key `vault` และ Schema 4 เหมือนเดิม การอัปหน้าแอปหรือ Service Worker ไม่ควรเปลี่ยนข้อมูลเหล่านี้เอง
