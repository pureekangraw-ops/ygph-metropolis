# วิธีอัปไฟล์ YGPH METROPOLIS

ชุดนี้ทำไว้ให้อัปทับของเดิม ไม่ต้องเปลี่ยนชื่อไฟล์ และไม่ต้องย้าย Path

## ก่อนอัป

1. เก็บสำเนาไฟล์เดิมไว้ก่อน
2. แตกไฟล์ `YGPH_METROPOLIS_UPLOAD.zip`
3. ตรวจว่าไฟล์หลักยังชื่อเดิม เช่น `index.html`, `app.js`, `highway-gate.js`, `flow-era.js`, `sw.js`

## ตรวจระบบ

ถ้าอัปผ่าน GitHub ให้รันคำสั่งนี้ที่โฟลเดอร์หลัก:

```bash
npm run deploy:gate
```

ต้องเห็นผลผ่าน 3 ส่วน:

- เทสต์ทั้งหมดผ่าน
- JavaScript Syntax ผ่าน
- ภาษาไทย UTF-8 ผ่าน

GitHub Workflow ชื่อ `Phase 1 Deploy Gate` จะตรวจซ้ำให้อัตโนมัติ แต่จะไม่ Deploy แทนคุณ

## ตอนอัป

1. คัดลอกไฟล์จาก ZIP ไปวางที่ Root ของ Repository
2. เลือกแทนที่ไฟล์เดิมทั้งหมด
3. ห้ามเติมเลขรุ่นต่อท้ายชื่อไฟล์
4. Push หรือ Deploy ด้วยวิธีเดิมของคุณ

## หลังอัป

1. เปิดแอปออนไลน์หนึ่งครั้ง เพื่อให้ไฟล์ Offline รุ่นใหม่โหลดครบ
2. ล็อกแล้วปลดล็อกแอป เพื่อตรวจว่าข้อมูลเดิมยังอยู่
3. ทดลองปิดและเปิดแอปใหม่
4. ดูหน้า “ตั้งค่าและเครื่องมือ” เพื่อตรวจสถานะอัปเดต

รุ่นนี้ไม่ใช้แคชเก่าที่เคยฉีด Script เป็น Rollback เพราะไฟล์อาจไม่ครบ รุ่นนี้จะเป็นฐานปลอดภัยชุดแรก และตั้งแต่การอัปเดตครั้งถัดไปจึงย้อนกลับรุ่นก่อนหน้าได้

ฐานข้อมูลยังใช้ชื่อ `stock-pocket-secure`, DB version 1, store `kv`, key `vault` และ Schema 4 เหมือนเดิม
