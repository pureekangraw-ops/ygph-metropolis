{
  "name": "ygph",
  "version": "2.1.4",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check:syntax": "node --check highway-gate.js && node --check app.js && node --check flow-era.js && node --check flow-era-3.5.js && node --check metropolis-v4.js && node --check sw.js",
    "check:utf8": "node scripts/verify-utf8.mjs",
    "deploy:gate": "npm test && npm run check:syntax && npm run check:utf8"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
